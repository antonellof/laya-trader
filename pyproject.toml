[project]
name = "laya-trader"
version = "0.1.0"
description = "Laya MLX decides BUY / SELL / HOLD every second from market signals (paper only)"
requires-python = ">=3.11"
dependencies = ["laya-mlx>=0.2.0,<0.3"]

[tool.uv]
package = false

[tool.ruff]
line-length = 100
